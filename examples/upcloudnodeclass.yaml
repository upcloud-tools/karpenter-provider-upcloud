apiVersion: karpenter.upcloud.com/v1alpha1
kind: UpCloudNodeClass
metadata:
  name: default
spec:
  zone: de-fra1
  plan: 2xCPU-4GB
  # Optional: root disk size (GB, default 20) and tier (standard|maxiops|hdd, default standard).
  storageGB: 20
  storageTier: standard

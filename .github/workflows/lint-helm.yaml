name: Lint Helm

on:
  push:
    branches:
      - main
    paths:
      - 'deploy/helm/**'
      - '.github/workflows/lint-helm.yaml'
  pull_request:
    branches:
      - main
    paths:
      - 'deploy/helm/**'
      - '.github/workflows/lint-helm.yaml'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  helm-lint:
    uses: upcloud-tools/gha-actions/.github/workflows/lint-helm.yaml@070918b27e20a6bd320cc93b569d912b1a227a6c
    with:
      helm_template_extra_args: --set config.clusterUUID=0d000000-0000-4000-8000-000160150100
